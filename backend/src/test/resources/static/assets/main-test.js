// gc-test-marker: main bundle js
// Fixture for path-matrix tests — verifies /assets/** requires an authenticated session
// (ADR-037 §2: the SPA bundle enumerates the application's UI surface and must not be
// fetchable by anonymous browsers).
export {};
