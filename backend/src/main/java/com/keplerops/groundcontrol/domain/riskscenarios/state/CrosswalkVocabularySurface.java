package com.keplerops.groundcontrol.domain.riskscenarios.state;

/**
 * Vocabulary surface that a crosswalk entry maps onto within a
 * {@code MethodologyProfile} (GC-T012 C2, C6). Declaration order matches the
 * mirrored MCP/frontend constant arrays (ADR-034).
 */
public enum CrosswalkVocabularySurface {
    INPUT_SCHEMA,
    OUTPUT_SCHEMA,
    TREATMENT_STRATEGY_VOCABULARY
}
