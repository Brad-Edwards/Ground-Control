### Added
- Traceability Matrix (GC-Q003): read-only `GET /api/v1/requirements/traceability/matrix` endpoint and a React workspace page rendering requirements against link-type columns with per-link-type coverage bars and ACTIVE-requirement gap highlighting. Filterable by wave, status, and link type. Exposed to agents via the `gc_traceability_matrix` MCP tool. (#689)
