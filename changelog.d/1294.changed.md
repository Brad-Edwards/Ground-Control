Add a CLD protected-path policy gate with scope-bound design-authority approval
markers for mixed implementation and protected contract, battery, registry,
policy, or workflow changes, using the base branch authority model after the
registry is bootstrapped, sanitized PR comments in CI, and an out-of-band MCP
approval grant before marker posting; switch the repo review-disposition config
to authoritative auto mode.
