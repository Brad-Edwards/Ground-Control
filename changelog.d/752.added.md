Added graph-native GRC interchange (GC-P012): a versioned `GrcInterchangeBundle` JSON envelope covering operational assets, risk scenarios, controls, findings, and evidence references; an exporter at `GET /api/v1/export/grc-interchange`; and an importer at `POST /api/v1/admin/import/grc-interchange` that upserts operational assets idempotently by external UID and persists a per-entity provenance shadow so client-supplied timestamps never overwrite domain timestamps (ADR-045).
