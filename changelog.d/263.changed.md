### Changed
- Internal: `GrcAnalysisService` now delegates to a `RiskAnalysisOrchestrator` facade; `GrcAnalysisServiceTest` mocks the facade instead of five individual services. No API or functional change. (#263)
- Refactored: `RiskHeatmapService.PlotAccumulator` now owns the row→band resolvers (`resolveLikelihood`, `resolveImpact`) that were previously dangling on the outer service; only the methodology-family + parsing helpers remain at the service level (Sonar S3398).
