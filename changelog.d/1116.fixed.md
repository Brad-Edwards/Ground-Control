### Fixed

- IaC/pipeline derivation config no longer sets `groundcontrol.derivation.iac.repository-root` in `application.yml`: a bare `.` could not be bound to a `Path` by Spring's web-aware `PathEditor` in a ServletContext, breaking application-context startup (OpenAPI contract generation and integration tests). The property now relies on its `Path.of(".")` field default.
