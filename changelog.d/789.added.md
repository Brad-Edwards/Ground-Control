Added a `DecisionAnalysisRecord` aggregate (GC-W011) that captures decision analyses; inputs, model name, simulation parameters, results, alternatives, chosen alternative, and rationale; under `/api/v1/decisions`. Adds a `DECISION_RECORD` artifact type so existing traceability links can target decisions.
