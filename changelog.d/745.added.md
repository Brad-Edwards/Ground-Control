Add FAIR materiality and loss taxonomy support (GC-T016): extend the seeded FAIR `MethodologyProfile` with explicit primary / secondary loss distinctions in `NormalizedConcept` (`PRIMARY_LOSS_MAGNITUDE`, `SECONDARY_LOSS_MAGNITUDE`), the full FAIR-MAM nine-loss canonical taxonomy (`FairLossForm`), and a stakeholder taxonomy for secondary-loss attribution (`FairStakeholderKind`). The FAIR input/output schemas now carry FAIR-MAM loss-form breakdowns on both primary and secondary lines, stakeholder-specific secondary effects, and percentile fields on LEF / LM / ALE. Materiality rides on the existing `RiskAssessmentResult` substrate as typed JSON—no parallel aggregate.
