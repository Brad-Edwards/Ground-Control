Cap caller-controlled WSJF Monte Carlo iterations on `/api/v1/backlog-items/{id}/wsjf` at `1_000_000` to prevent authenticated-DoS heap exhaustion, and overwrite the `attributedTo` field on every posted Cost-of-Delay component with the authenticated principal (ADR-033) so the audit trail cannot be poisoned with a forged contributor.
