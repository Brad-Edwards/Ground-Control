Harden FAIR quantitative analysis monetary-scale handling (GC-T011 / GC-T016): `FairQuantitativeAnalysisService.toItem` now reads `secondary_loss_magnitude` currency and scale independently from `primary_loss_magnitude`, normalizes secondary samples into the primary scale when the two differ, and refuses to combine across currencies—dropping the secondary contribution from the LM / ALE rollup and emitting an explicit limitation rather than silently mixing magnitudes. Asymmetric secondary input (exactly one of `secondary_loss_event_frequency` / `secondary_loss_magnitude`) now records a limitation and drops the half-input from the simulation. Unit and controller tests assert primary and secondary loss envelopes are surfaced as independent fields with distinct percentile values, preventing a regression that would null either field or alias both at the same envelope.
