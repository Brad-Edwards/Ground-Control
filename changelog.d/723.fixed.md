Repoint SonarCloud analysis to the `autarchy-ai` org / `autarchy-ai_Ground-Control` project so CI sonar analysis and PR decoration work after the repository moved to the `autarchy-ai` GitHub organization. Updates `backend/build.gradle.kts`, `sonar-project.properties`, `.ground-control.yaml`, `.github/workflows/ci.yml`, `.mcp.json`, and the README badge.
