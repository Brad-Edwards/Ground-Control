Fix `GrcInterchangeExporter` to encode the Control's structural classifier (function plus optional category) into the bundle's `controlType` field instead of the lifecycle status, restoring round-trip integrity. Malformed GRC interchange uploads now return HTTP 400 with a stable opaque envelope so Jackson parse details cannot reflect attacker-controlled bytes back to the caller.
