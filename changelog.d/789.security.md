Stamp the authenticated principal into the reserved `_attributedTo` key on the decision-analysis `inputs` map so wire-supplied attribution can never overwrite the actor sourced from `ActorHolder` (ADR-033).
