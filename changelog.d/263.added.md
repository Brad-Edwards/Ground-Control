### Added
- Five methodology-attributed aggregate risk reporting projections under `/api/v1/analysis/grc/` (`risk-heatmap`, `risk-distribution`, `risk-top-n`, `risk-trends`, `risk-posture`) and matching `gc_analyze` MCP kinds (`risk_heatmap`, `risk_distribution`, `risk_top_n`, `risk_trends`, `risk_posture`), each carrying ADR-035 envelope fields and explicit `limitations` when a methodology is incompatible with the requested view (GC-T008). (#263)
