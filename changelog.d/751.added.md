### Added
- GRC Portfolio Reporting Views (GC-Q013): read-only `GET /api/v1/analysis/grc/portfolio` endpoint (fifth `GrcAnalysisService` delegate) and a React portfolio dashboard rolling the graph up into risk posture, control health, evidence freshness, finding trends, asset criticality concentration, and methodology-family (FAIR/NIST/ISO) summaries, each carrying actionable drill-down id lists behind its key signal counts (critical assets, unmapped and unassessed controls, overdue register reviews, open and overdue findings). Pure read-only projection with no new tables or materialized views (GC-L007); evidence-freshness counts reuse `EvidenceFreshnessAnalysisService`. Filterable by `asOf` and freshness window. Exposed to agents via the `gc_analyze` `portfolio` kind. (#751)
