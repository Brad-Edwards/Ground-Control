fixed: ComplianceFrameworkMapping aggregate (GC-I002 / GC-I005 / GC-I007 / GC-L011) adversarial-review hardening. Scope `(endpoint, framework, framework_element)` uniqueness to each endpoint side via partial UNIQUE indexes so two control-side rows (including compensating controls) can map to the same SOC2 / ISO 27001 element without a `NULLS NOT DISTINCT` collision on the polymorphic NULL side. Restore the `containsControlChars` log-injection guard on the `ComplianceFrameworkMappingService.update` path so PUT cannot land embedded `\n` / `\r` / `\t` in `frameworkIdentifier`. Repair the `gc_compliance_framework_mapping action=update` MCP handler so it no longer throws `keys is not iterable` at the trust boundary (the rename-object form of `pick` was broken at runtime). Add MCP smoke tests for create / update / error paths and ArgumentCaptor-backed controller tests that pin every request-to-command field-pass-through and every query-param parse on `/api/v1/analysis/grc/{compliance-posture,framework-gap}`.
