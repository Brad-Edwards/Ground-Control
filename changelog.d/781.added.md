Added Cost-of-Delay / WSJF backlog prioritization (GC-W003): a `BacklogItem` aggregate with four probability-distribution-valued estimate components, a seeded Monte Carlo path that computes a WSJF distribution per item, and a ranking-delta analysis comparing two snapshots. New REST surface under `/api/v1/backlog-items`.
