### Fixed
- Aggregate risk reporting envelopes now apply the methodology-profile filter before computing `byFamily` so a heatmap restricted to a non-FAIR profile no longer surfaces a `FAIR_INCOMPATIBILITY` limitation referencing rows the caller never asked about, always carry the requested `methodologyProfileId` (and family when resolvable via the repository) when the caller restricts by profile, scope the top-N `MIXED_METHODOLOGY` limitation to families that actually appear in the returned entries instead of every considered row, and always surface a `risk-trends` limitation documenting that the audit table for `RiskRegisterRecord` does not carry `project_id` so revisions of deleted records cannot be counted (GC-T008). (#263)
