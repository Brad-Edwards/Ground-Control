### Fixed
- Resolve transactional self-injection (Sonar S6809) in `KeyRiskIndicatorService`, `RiskAppetiteProfileService`, and `RiskAssessmentCampaignService` by calling the repository directly in write-path methods. (#754)
- Refactor `KeyRiskIndicator.classify()` and `RiskAppetiteEvaluator` evaluation methods to reduce return-count per method (Sonar S1120). (#754)
- Rename `record` local variable in `KeyRiskIndicatorService` to avoid restricted identifier (Sonar). (#754)
- Add unit tests for `KeyRiskIndicatorService`, `RiskAppetiteProfileService`, and `RiskAssessmentCampaignService` to raise new-code coverage above 80%. (#754)
- Fix test smells: parameterize monetary-band evaluator tests, remove commented-out code blocks, simplify useless `eq()` invocations. (#754)
- Refactor `KeyRiskIndicator.classify()` and all four `RiskAppetiteEvaluator` evaluation methods to extract private single-return helpers (Sonar MAJOR S1120). (#754)
- Clean up test smells: consolidate three monetary-band tests into a single `@ParameterizedTest`, remove commented-out code blocks, simplify useless `eq()` invocations in `RiskAssessmentCampaignControllerTest`. (#754)
- Add `@WebMvcTest`-independent unit tests for `KeyRiskIndicatorService`, `RiskAppetiteProfileService`, and `RiskAssessmentCampaignService` to raise Sonar new-code coverage above the 80% gate threshold. (#754)
- Extract duplicate not-found message string literals into private constants in `KeyRiskIndicatorService`, `RiskAppetiteProfileService`, and `RiskAssessmentCampaignService` (Sonar S1192). (#754)
- Refactor `RiskAppetiteEvaluator` classify helpers to stay within the 3-return limit per method (Sonar S1142). (#754)
- Hoist command-object construction outside `assertThatThrownBy` lambdas in RGL service tests so each lambda contains only the single throwing service call (Sonar S5778). (#754)
- Pin `JAVA_HOME` to Java 21 in the spotless and gradle-check pre-commit hooks so they run correctly when the system default is Java 25. (#754)
