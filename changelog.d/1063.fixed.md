### Fixed
- Updated `MigrationSmokeTest` version list to include V131 and V132 (FAIR-CAM control domain migrations for GC-I017). (#1063)
- Refactored `FairQuantitativeAnalysisService` to extract `toItem` Brain Method into named phase methods, introduce `FairCalibrationInputs` record for the 9-parameter `simulate` call, replace duplicate string literals with constants, use `Math.clamp` instead of `Math.min/max` nesting, and collapse `scaleMultiplier` switch to a single-return form. (#1063)
- Replaced duplicate `"fraction (0.0–1.0) per dimension"` literals in `MethodologyProfileService` with a named constant. (#1063)
- Simplified `assertThatThrownBy` lambda in `FairQuantitativeAnalysisServiceTest` to use `catchThrowableOfType` per SonarCloud S6204. (#1063)
