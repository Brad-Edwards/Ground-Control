### Changed

- Internal: extracted the duplicated `current_stage` / `gate_point` error-detail keys to constants in `ResearchRunService` and narrowed the research-run test exception-assertion lambdas to a single throwing call (SonarCloud code-smell cleanup; no behavior change).
